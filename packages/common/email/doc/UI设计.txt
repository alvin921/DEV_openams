1. 检查用户信息是否有效（userpass文件是否存在），无效则加载账号添加界面，有效则加载邮箱列表界面

2. 账号添加界面

	显示名
	电子邮件
POP3服务器（必填）
	主机名
	用户名
	密码

SMTP服务器
	主机名
	用户名
	密码

如果未指定SMTP服务器用户名和密码，则使用POP3的设置，设置完毕，自动进入邮箱列表界面

3. 邮件列表界面
	第一次进入和主动刷新时要启动收取邮件，以后实现成定时刷新收取。

4. 邮件查看界面
	第一阶段Demo，只显示邮件源码
	第二阶段Demo，做简单解析（仅显示text或html中text，以及附件）

